[build]
builder = "NIXPACKS"

[build.nixpacksOptions]
buildCommand = "npm install"

[deploy]
startCommand = "npm start"
healthcheckPath = "/"
restartPolicyType = "ON_FAILURE"

[deploy.variables]
NODE_ENV = "production"
PORT = "3000"